function arrayManipulator(integers, stringCommands) {

    for (let i = 0; i < integers.length; i++) {
        let currentCommand = stringCommands[i].split(' ');
        let index
    }

}


arrayManipulator([1, 2, 4, 5, 6, 7],
    ['add 1 8', 'contains 1', 'contains 3', 'print']
)